# Check hacked status via git for a given `-dev` project.

Notice: currently the root path and temp directory are hardcoded. 

## Examples

Check status of views module.

```
bash check_hacked.sh views
```